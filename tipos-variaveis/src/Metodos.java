public class Metodos {

    //qualquer ação é considerada metodo // atributos na maioria das vezes são variaves

    //deve ser nomeado com verbo 
    // seguir o padrão camel cacase (todas as letras minusculas, com exceção da primeira letra da segunda palavra )

    // o metodo precisa ter uma unica responsabilidade EX : ou calcular ou imprimir 

    // os metodos precisam estar dentro de uma classe 

    // qual a principal proposta do metodo ? se perguntar constantemente até compreender a real finalidade do mesmo

    // qual o tipo de retorno esperado após executar o metodo ? você deve analisar se o metodo será responsavel por retornar algum valor ou não 

    // Quais os parametros necessarios para execução do metodo ? Os metodos as vezes vão precisar de argumentos como criterios para execução 

    // O metodo possui o risco de apresentar alguma exceção ? Exceções são comuns na execução de metodos, as vezes é necessario prever e tratar a possivel existencia de uma exceção

    // qual a visibilidade do metodo ? Será necessario que o metodo seja visivel a toda aplicação, somente em mesmo pacotes, através de herança ou somente a nivel da propria classe ?

    // public ou private
    

    
}
